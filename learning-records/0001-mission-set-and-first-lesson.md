# 0001 — Mission set; started at components

**Date:** 2026-07-13

## Context
First session. Workspace was empty.

## Decisions
- **Mission:** get job-ready as a React developer.
- **Background:** comfortable *reading* JS but shaky on fundamentals (function
  syntax, `.map`/`.filter`, destructuring, async). New to React.
- **Stance:** shore up JS gaps inline as they surface in React context, not via
  a separate JS course first. Teach modern function components + hooks only.

## Taught
- Lesson 0001: components as functions returning JSX, capitalization rule,
  nesting, single-root / Fragment rule. Quiz-based retrieval practice.

## Zone of proximal development / next
- Next lesson: **props** (passing data in). Watch for the JS destructuring gap —
  it'll show up in `function Card({ name })` and likely needs a shore-up callout.
- Then: rendering lists with `.map()` (directly hits a known JS weak spot),
  then state/`useState`, then events, then a data fetch.
